{% do lms_auth_config.update(EDXAPP_LMS_AUTH_EXTRA) %}
{% for key, value in lms_auth_config.items() -%}
  {% if value == 'None' -%}
    {% do lms_auth_config.update({key: None }) %} 
  {%- endif %}
{%- endfor %}
{{ lms_auth_config | to_nice_json }}
