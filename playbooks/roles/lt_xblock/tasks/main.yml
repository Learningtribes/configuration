---
- name: Install xblock
  pip:
    name: "git+http://{{git_username}}:{{git_password}}@github.com/Learningtribes/{{ XBLOCK_NAME }}.git@master"
    virtualenv: "{{ edxapp_venv_dir }}"
    state: forcereinstall
  become_user: edxapp
  tags: ['never', 'deploy-xblock']


